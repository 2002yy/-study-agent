# Copy to .env.local through tools/manage-searxng.ps1. Never commit the local file.
SEARXNG_SECRET=replace-with-a-random-local-secret

# Optional machine-local outbound proxy settings. Keep them out of Git.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=127.0.0.1,localhost
